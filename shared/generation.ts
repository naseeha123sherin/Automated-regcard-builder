import { clone, type Project, type Templates } from './model';
import { safeName } from './project';
import { generateRdl, validateRdl } from './rdl';
import { readiness, validateConfig } from './validation';
export function generateFiles(p: Project, t: Templates) { const xml = generateRdl(p), rdl = validateRdl(xml, p); const issues = [...readiness(p), ...validateConfig(p.regcardConfig, 'regcard', t.regcard), ...validateConfig(p.checkinTermsConfig, 'checkin'), ...validateConfig(p.checkoutTermsConfig, 'checkout')]; const prefix = safeName(p.propertyName); return { rdl, issues, valid: rdl.valid && !issues.some(x => x.severity === 'error'), files: [{ name: `${prefix ? prefix + '_' : ''}RegistrationCard.rdl`, content: xml, mime: 'application/xml' }, { name: `${prefix ? prefix + '_' : ''}regcard.json`, content: JSON.stringify(clone(p.regcardConfig), null, 2), mime: 'application/json' }, { name: `${prefix ? prefix + '_' : ''}checkin_terms.json`, content: JSON.stringify(clone(p.checkinTermsConfig), null, 2), mime: 'application/json' }, { name: `${prefix ? prefix + '_' : ''}checkout_terms.json`, content: JSON.stringify(clone(p.checkoutTermsConfig), null, 2), mime: 'application/json' }] }; }
